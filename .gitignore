# Xcode / build
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# macOS
.DS_Store

# Swift Package Manager
.swiftpm/
.build/

# Secrets / sessions (never commit credentials or tokens)
*.ig_session.json
.ig_session.json
secrets.json

# personal source captures (room photos) — keep out of the repo
incoming/

# installed agent skills (dev tooling, not app code)
.agents/
